{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && NODE_PATH=./node_modules npx tsx ../scripts/migrate.ts && NODE_PATH=./node_modules npx tsx ../scripts/seed.ts && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
